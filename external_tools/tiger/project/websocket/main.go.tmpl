// WebSocket main.go 模板
package main

import (
	"github.com/karldoenitz/Tigo/web"
	"{{ .PackageName }}/handler"
)

// WebSocket 路由
var urls = []web.Pattern{
	{"/ws", handler.WebSocketHandler{}, nil},
}

func main() {
	application := web.Application{
		IPAddress:   "{{ .IP }}",
		Port:        {{ .Port }},
		UrlPatterns: urls,
	}

	{{ if .EnableLogger }}
	// 配置日志文件路径
	// application.ConfigPath = "config.json"
	{{ end }}

	application.Run()
}